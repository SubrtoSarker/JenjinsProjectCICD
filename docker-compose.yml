services:
  projectmanagement:
    build:
      context: .
      dockerfile: ProjectManagement/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
    restart: unless-stopped
